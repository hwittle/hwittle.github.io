
  # Portfolio (Technical & UX Writer)

  This is a code bundle for Portfolio (Technical & UX Writer). The original project is available at https://www.figma.com/design/S4DCuR72de4tmVoZ7jPfYx/Portfolio--Technical---UX-Writer-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  